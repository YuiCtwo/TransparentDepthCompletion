general:
  log_dir: /home/ctwo/VDCNet/logs
  checkpoint_dir: /home/ctwo/VDCNet/logs/checkpoints
  model: DFNet
  frame_h: 240
  frame_w: 320
  pretrained_weight: /home/ctwo/VDCNet/pretrained/df_pt_sn.pth
#  pretrained_weight: /home/ctwo/VDCNet/logs/checkpoints/ckpt-best.pth
  finetune: False
  running_type: train
  print_freq: 10

# data settings
dataset:
  name: blenderglass
  dataset_dir: /home/ctwo/blender_glass_data
  type: pic
  data_aug:
    depth_norm: True
    rgb_aug: False

training:
  train_batch_size: 8
  valid_batch_size: 16
  use_multi_gpu: True
  epochs: 15
  lr: 0.0004
  num_workers: 8
  decay_epochs: 20
  decay_gamma: 0.5
  save_epochs: 10

model:
  depth_plane_num: 50
  pnet:
    type: VPointNet # VPointNet
    hidden_dim: 32
    out_channels: 32

  color:
    type: PyramidPool
    out_channels: 32

  normal:
    input_dim: 1
    refine: False


loss:
  type: ["consistency_loss"]
  weight: [0.4]

metrics:
  type: ["masked_depth_rmse", "masked_depth_mae", "masked_depth_rel", "depth_failing_105", "depth_failing_110", "depth_failing_125"]
